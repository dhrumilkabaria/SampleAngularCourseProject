export class Ingredient {
    // public name: string;
    // public amount: number;
    // we can directly write access specifier in the constructor argument itself and then no need to declare data memebr in the class.
    constructor(public name: string, public amount: number) {

    }
}
